# Database (SQLite for dev, PostgreSQL for production)
DATABASE_URL="file:./db/custom.db"

# For production PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/rise_foods_erp?schema=public"
